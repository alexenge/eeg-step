Setting up the environment and repo
1. Clone the git repo: git clone URL
2. Install package 'hatch': https://hatch.pypa.io/latest/install/
3. Test installation via Terminal: hatch run test
4. Select hatch Python environment (for VScode Users -> google if necessary)
